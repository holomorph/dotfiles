#!/bin/bash
# pomf: post files to http://pomf.se/

upload=$(curl -sF "files[]=@$1" https://pomf.cat/upload.php)
geturl=$(jshon -e files -e 0 -e url -u <<< "$upload")

printf "%s/%s\n" "https://a.pomf.cat" "$geturl"
