# ~/.mutt/gmail.muttrc

color status brightgreen black

set realname  = $my_realname
set from      = $my_gmail
set folder    = "~/.mutt/mail/"
set sendmail  = "/usr/bin/msmtp -a gmail -f $my_gmail --user=$my_gmail"
set spoolfile = "+inbox"
set postponed = "+drafts"

unset record # gmail saves sent mail in +sent anyways

mailboxes +inbox +amazon +cycling +drafts \
          +sent +spam +thesis +trash

# vim:ft=muttrc
