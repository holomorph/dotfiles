[Unit]
Description=DHCP Client Daemon on All Interfaces
Wants=network.target
Before=network.target

[Service]
Type=forking
PIDFile=/run/dhcpcd.pid
ExecStart=/usr/bin/sudo dhcpcd -q -b
ExecStop=/usr/bin/sudo dhcpcd -x

[Install]
WantedBy=default.target
